# 🚀 快速部署指南

## 推荐部署方式

### 方式1：一键部署脚本（推荐）

```bash
# 1. 进入项目目录
cd tech-news-automation

# 2. 运行部署脚本
bash deploy.sh

# 3. 按提示选择部署方式
```

### 方式2：Docker Compose（最简单）

```bash
# 1. 配置API密钥
nano .env

# 2. 启动服务
docker-compose up -d

# 3. 查看日志
docker-compose logs -f
```

### 方式3：本地部署

```bash
# 1. 安装依赖
pip3 install -r requirements.txt
sudo apt-get install fonts-noto-cjk

# 2. 配置API密钥
nano run.sh

# 3. 测试运行
python3 main.py --test

# 4. 设置定时任务
crontab -e
# 添加: 30 8 * * * cd $(pwd) && ./run.sh
```

---

## 📋 部署前准备

### 1. 获取API密钥（必需）

| API | 注册地址 | 免费额度 |
|-----|---------|---------|
| NewsAPI | https://newsapi.org/ | 100请求/天 |
| GNews | https://gnews.io/ | 100请求/天 |

### 2. 配置密钥

编辑 `.env` 或 `run.sh`：

```bash
NEWSAPI_KEY=b1b5dc1e64064cddb26ab4d984642ba3
GNEWS_KEY=626d1ce5f0c532755f3952c362034952
```

---

## 🐳 Docker部署详解

### 构建镜像

```bash
docker build -t tech-news-automation .
```

### 运行容器

```bash
docker run -d \
  --name tech-news \
  -e NEWSAPI_KEY="your_key" \
  -e GNEWS_KEY="your_key" \
  -v $(pwd)/output:/app/output \
  tech-news-automation
```

### 使用Docker Compose

```bash
# 启动
docker-compose up -d

# 停止
docker-compose down

# 查看日志
docker-compose logs -f

# 重启
docker-compose restart
```

---

## ⏰ 定时任务设置

### Linux Crontab

```bash
# 编辑crontab
crontab -e

# 每天8:30运行
30 8 * * * cd /path/to/tech-news-automation && ./run.sh
```

### macOS launchd

创建 `~/Library/LaunchAgents/com.technews.daily.plist`：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.technews.daily</string>
    <key>ProgramArguments</key>
    <array>
        <string>/path/to/tech-news-automation/run.sh</string>
    </array>
    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key>
        <integer>8</integer>
        <key>Minute</key>
        <integer>30</integer>
    </dict>
</dict>
</plist>
```

加载：

```bash
launchctl load ~/Library/LaunchAgents/com.technews.daily.plist
```

---

## 📁 部署后的目录结构

```
tech-news-automation/
├── output/              # 生成的图片和笔记
│   ├── tech_news_cover.jpg
│   ├── tech_news_detail.jpg
│   ├── tech_news_summary.jpg
│   └── 20260221/        # 每日笔记目录
├── logs/                # 运行日志
│   └── tech-news-20260221.log
├── cache/               # 缓存文件
└── .env                 # 环境变量配置
```

---

## 🔍 验证部署

### 1. 测试运行

```bash
python3 main.py --test
```

### 2. 检查输出

```bash
ls -la output/
```

### 3. 查看日志

```bash
tail -f logs/tech-news-$(date +%Y%m%d).log
```

### 4. 检查定时任务

```bash
crontab -l
```

---

## 🐛 常见问题

### Q: 定时任务不执行？

```bash
# 检查cron服务
sudo service cron status
sudo service cron start

# 查看日志
grep CRON /var/log/syslog
```

### Q: 中文显示乱码？

```bash
# 安装字体
sudo apt-get install fonts-noto-cjk
fc-cache -fv
```

### Q: API请求失败？

```bash
# 测试API
curl "https://newsapi.org/v2/everything?q=test&apiKey=YOUR_KEY"

# 检查网络
ping newsapi.org
```

---

## 📞 需要帮助？

- 查看完整文档：`部署指南.md`
- 查看使用说明：`使用指南.md`
- 查看API配置：`API配置指南.md`

---

**部署完成后，明天早上8:30将自动生成第一份科技新闻早报！**
